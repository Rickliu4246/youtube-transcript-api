{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
```

5. **Commit changes**：
   - Commit message: `Add vercel.json configuration`
   - 點擊 "Commit changes"

---

## 🎯 這個配置做什麼？

- **builds**: 告訴 Vercel 使用 Node.js 來處理 `api` 資料夾中的文件
- **routes**: 設置路由規則，將請求導向正確的 API 端點

---

## ⏱️ 完成後

1. **等待 1-2 分鐘**讓 Vercel 重新部署
2. **查看 Deployments 頁面**，應該會看到新的部署（狀態變成綠色 "Ready"）
3. **測試 API**：
```
   https://youtube-transcript-api-dusky.vercel.app/api/transcript?video_id=dQw4w9WgXcQ&lang=en
```

---

## 📝 你現在的文件結構

部署完成後應該有：
```
youtube-transcript-api/
├── api/
│   ├── index.js
│   └── transcript.js
├── .gitattributes
├── package.json       ✅
└── vercel.json        ← 現在要創建這個
